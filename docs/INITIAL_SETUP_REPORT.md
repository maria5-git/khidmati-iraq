 تقرير الإعداد الأولي (Initial Setup Report)

التاريخ: 2026-09-01
المطور: ماريا هيثم
المشروع: Khidmati Iraq - Backend

1. إعداد بيئة التطوير
- لغة البرمجة (Python): الإصدار 3.13.7.
- البيئة الافتراضية (Virtual Environment): تم إنشاؤها وتفعيلها بنجاح باستخدام مجلد `.venv`.
- مدير الحزم (pip): تم تحديثه إلى أحدث إصدار.

2. إعداد قاعدة البيانات (PostgreSQL)
- قاعدة البيانات الرئيسية: تم إنشاء قاعدة البيانات `khidmati_iraq` وتم الاتصال بها بنجاح.
- قاعدة بيانات الاختبارات: تم إنشاء قاعدة البيانات `khidmati_iraq_test` يدوياً (لضمان تشغيل اختبارات `pytest` بدون أخطاء).
- الهجرات (Migrations): تم تنفيذ هجرات `Alembic` بنجاح باستخدام الأمر `alembic upgrade head`، مما أدى إلى إنشاء جميع الجداول (Users, Reports, Comments, etc.).
- البيانات الأولية (Seed): تم تنفيذ سكربت التعبئة `python -m scripts.seed` بنجاح، وتمت إضافة المحافظات (Governorates)، والتصنيفات (Categories)، وحسابات الاختبار (Admin, Employee, Citizen).

3. تشغيل التطبيق (Application Server)
- الخادم (Server): تم تشغيل التطبيق باستخدام `uvicorn app.main:app --reload`.
- التوثيق التفاعلي (Swagger UI): يعمل على الرابط `http://127.0.0.1:8000/docs` ويظهر جميع نقاط النهاية (Endpoints).
- الاتصال بقاعدة البيانات: تأكد من الاتصال بقاعدة البيانات عبر نقطة نهاية الـ `/` والتي أعادت رسالة نجاح (Status 200 OK).

4. اختبار الحسابات الأولية (Seeded Accounts)
تم تسجيل الدخول إلى الحسابات التالية عبر واجهة `/docs` بنجاح:
- `admin@khidmati.local` (دور مدير / Admin)
- `employee.baghdad@khidmati.local` (دور موظف / Employee - محافظة بغداد)
- `citizen1@khidmati.local` (دور مواطن / Citizen)

5. حالة الاختبارات الآلية (Test Suite)
- الأمر المستخدم: `pytest -v`
- النتيجة: نجاح 11 اختبار بالكامل (`11 passed, 0 failed`).
- ملاحظة: ظهرت 6 تحذيرات (Warnings) متعلقة بمكتبات الطرف الثالث (مثل `datetime.utcnow()` و `on_event`)، ولكنها لا تؤثر على أداء التطبيق أو صلاحية الاختبارات.

6. ملاحظات فنية على الإعداد (Technical Observations)
- نظراً لاستخدام إصدار Python 3.13، واجهت مشكلة في تثبيت المكتبة `psycopg2-binary` (غير متوافقة مع هذا الإصدار)، لذا قمت بحل المشكلة عن طريق استخدام مكتبة `asyncpg` كبديل وتعديل روابط الاتصال في ملف `.env` إلى الصيغة `postgresql+asyncpg://...`.
- تطلب تشغيل الاختبارات إنشاء قاعدة بيانات `khidmati_iraq_test` يدوياً، وتم ذلك عبر أمر `psql`.

- الخلاصة
تم إعداد المشروع بالكامل، ويعمل خادم التطوير دون أخطاء حرجة، وجميع الاختبارات الحالية تجتاز بنجاح. المشروع جاهز تماماً لبدء مرحلة التطوير وحل المهام المطلوبة.

7. تطور المشروع - المهمة الثانية (TASK-02): تأمين ملكية البلاغ للمواطن
 
التاريخ: 2026-09-02

الهدف من المهمة:
منع المواطن من الوصول إلى بلاغات المواطنين الآخرين (سد ثغرة أمنية خطيرة كانت موجودة في الكود الأساسي).

التحديات التي واجهتني أثناء التنفيذ:
-خطأ في الاستيراد (ImportError): عند البدء بالتعديل، واجهت خطأ ImportError: cannot import name 'ReportOut'. تبين أن الاسم الصحيح للـ Schema في ملف app/schemas/report.py هو ReportDetailResponse و ReportResponse وليس ReportOut.
-تضارب في التوكن (Authorization): كنت أحاول استخدام get_current_active_citizen في نقاط النهاية، لكن المشروع يستخدم require_citizen كـ Dependency موحدة، لذا قمت بتوحيدها لتجنب الأخطاء.

الإجراءات التي قمت بها لحل المشكلة:
-قمت بإعادة كتابة ملف app/api/v1/reports.py بالكامل، وعدلت جميع نقاط النهاية (GET, PATCH, POST /cancel, /comments, /history) لتستخدم دالة تحقق جديدة.
-أضفت دالة جديدة في app/services/report_service.py باسم get_citizen_report_or_404، والتي تبحث عن البلاغ وتتأكد من أن citizen_id الخاص به يطابق المستخدم الحالي قبل إرجاعه، وإلا ترفع خطأ 404 Not Found.
-عدلت دوال update_report و cancel_report في الـ Service لتقبل citizen_id كمعامل إضافي لتطبق التحقق أيضاً على مستوى منطق الأعمال (Service Layer).

كيف اختبرت المهمة يدوياً؟
-سجلت الدخول كـ citizen1@khidmati.local وأنشأت بلاغاً جديداً.
-سجلت الخروج، ثم سجلت الدخول كـ citizen2@khidmati.local.
-حاولت الوصول إلى نفس البلاغ عبر GET /api/v1/reports/{id}.
-النتيجة: حصلت على استجابة 404 Not Found مع رسالة {"detail": "Report not found"}، مما يؤكد أن المواطن الثاني لم يتمكن من رؤية بلاغ الأول. 

نتيجة اختبارات Pytest بعد التعديل:
11 passed, 0 failed (اجتازت جميع الاختبارات دون أن تؤثر تعديلاتي على الوظائف القديمة).


8. التحقق من صحة المحافظة والمنطقة TASK-03: (Validate Governorate and Area Relationsh)

التاريخ: 2026-09-03

الهدف: منع إنشاء بلاغ بمنطقة لا تنتمي إلى المحافظة المختارة.

الإجراءات التي قمت بها:
1. عدلت دالة `validate_location` في `app/services/report_service.py`.
2. أضفت التحقق التالي: إذا كان `area.governorate_id` لا يساوي `governorate_id` المطلوب، يتم رفع خطأ `BadRequestError` مع رسالة توضيحية.

نتيجة الاختبار اليدوي:
- عند إرسال `governorate_id=1` (بغداد) و `area_id` تابع لمحافظة أخرى، ظهر الخطأ `"Area does not belong to the selected governorate"`. 
- عند إرسال منطقة صحيحة تابعة لنفس المحافظة، تم إنشاء البلاغ بنجاح. 

9. TASK-04: Complete Report Assignment (إكمال تعيين البلاغ)

التاريخ: 2026-09-03

الهدف: ضمان أن المدير يمكنه تعيين بلاغ لموظف صالح فقط، مع التحقق من المحافظة وتسجيل الحالة في السجل التاريخي.

الإجراءات التي قمت بها:
1. عدلت دالة `admin_assign_report` في `app/services/report_service.py`.
2. أضفت التحقق من أن الموظف يتبع نفس محافظة البلاغ (`employee.governorate_id == report.governorate_id`).
3. أضفت استدعاء دالة `record_status_change` لتسجيل التغيير في سجل التاريخ.
4. ضمنت أن جميع التغييرات تتم في معاملة واحدة (Transaction).

نتيجة الاختبار اليدوي:
- تم تعيين موظف تابع لنفس المحافظة بنجاح، وظهرت الحالة `assigned` وظهر السجل في `/history`. 
- محاولة تعيين موظف من محافظة أخرى أدت إلى خطأ `400` مع رسالة توضيحية. 

TASK-05 (إضافة): تعديل صلاحيات عرض سجل التحديثات (`/history`)

التاريخ: 2026-09-04

الموقف: أثناء اختبار TASK-05، اكتشفنا أن نقطة نهاية عرض سجل التحديثات (`GET /api/v1/reports/{id}/history`) كانت مقيدة فقط بالمواطن صاحب البلاغ، بينما يحتاج المدير والموظف أيضاً إلى رؤية هذا السجل لأغراض المتابعة والإدارة.

التعديلات التي قمت بها:

1. في ملف `app/api/v1/reports.py`:
   - أضفت استيراد `get_current_user` من `app.core.dependencies`.
   - أضفت استيراد `NotFoundError` و `PermissionDeniedError` من `app.core.exceptions`.
   - عدلت دالة `get_report_history` لتستخدم `get_current_user` بدلاً من `require_citizen`.
   - أضفت منطق صلاحيات يسمح بالوصول لـ:
     - المواطن صاحب البلاغ.
     - الموظف الذي يتبع نفس محافظة البلاغ.
     - المدير (صلاحية مطلقة).
   - رفعت خطأ `PermissionDeniedError` إذا لم يتحقق أي من الشروط.

2. في ملف `app/core/dependencies.py`: تأكدت من وجود دالة `get_current_user` (كانت موجودة بالفعل).

3. في ملف `app/core/exceptions.py`: تأكدت من وجود `NotFoundError` و `PermissionDeniedError` (كانتا موجودتين بالفعل).

نتيجة الاختبار:

- سجلت الدخول كمدير (`admin@khidmati.local`) وفتحت نقطة `/history` لأي بلاغ، فظهرت لي بيانات السجل بنجاح. 
- سجلت الدخول كمواطن (`citizen1`) وفتحت `/history` لبلاغ يخصني، فظهرت لي البيانات بنجاح. 
- سجلت الدخول كمواطن (`citizen2`) وحاولت فتح `/history` لبلاغ ليس لي، فظهر خطأ `PermissionDeniedError`. 



*ملاحظة:تم الاستعانة بالذكاء الاصطناعي في كتابة هذا التقرير.